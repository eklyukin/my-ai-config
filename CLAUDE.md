# Claude Code Instructions

Read and follow [`AGENTS.md`](AGENTS.md) as the canonical repository-wide AI
instructions before working in this repository.

Repository-tracked instructions take priority over supplementary files under
`.context/`. Load only the task-relevant files linked from
`.context/AGENTS.md`; do not load the entire local context tree by default.
Write every file under `.context/` in English regardless of the conversation
language.
